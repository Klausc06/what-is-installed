# Phase 6: Quick Detect Mode — Plan

**Date:** 2026-05-17
**Phase:** 6 — Quick Detect Mode
**Mode:** standard (single feature, no research needed)

## Task Breakdown

### T1: Add interactive flow to entry point
**File:** `bin/what-is-installed`
**Changes:**
1. After Pass 1 (line ~115), detect TTY via `[[ -t 1 ]]`
2. If TTY: render scan table (version=?), print "Found N tools.", prompt "Probe versions? [y/N] " to stderr
3. Read one line from stdin; if matches `[yY]`, proceed to Pass 2 (providers + probe)
4. If non-TTY or user says no: skip Pass 2, exit after scan table
5. If user says yes: run providers, Pass 2, render full table again (appended)
**Estimate:** ~20 lines added

### T2: Adapt render_table for scan mode
**File:** `lib/render.sh`
**Changes:**
1. `render_table()` checks a new variable `SCAN_MODE` (or similar)
2. In scan mode: version column shows `?` for all items
3. In full mode: behavior unchanged
**Estimate:** ~5 lines

### T3: Skip providers in scan mode
**File:** `bin/what-is-installed`
**Changes:**
1. Move `resolve_providers` call to after the prompt (only if user says yes)
2. Currently it runs unconditionally at line ~49 — make it conditional
**Estimate:** ~3 lines

### T4: Update existing tests
**File:** `tests/run.sh`
**Changes:**
1. `test_path_order_keeps_first_directory` — adapt to new default (no version column expected)
2. Add `--full` equivalent by setting TTY=1 or using a test mode env var
**Estimate:** ~10 lines

### T5: Add scan-only tests
**File:** `tests/run.sh`
**Changes:**
1. `test_scan_mode_output` — verify version column shows `?` in scan output
2. `test_scan_mode_fast` — verify scan completes < 2s (performance assertion)
3. `test_non_tty_no_prompt` — verify non-TTY mode does not print prompt
**Estimate:** ~30 lines

### T6: Integration verification
**Steps:**
1. Run `NO_COLOR=1 bash bin/what-is-installed` in TTY — verify scan table + prompt
2. Pipe: `NO_COLOR=1 bash bin/what-is-installed | head` — verify no prompt, scan only
3. Existing tests pass
4. Shellcheck clean
**Estimate:** manual verification

## Files Modified

| File | Change |
|------|--------|
| `bin/what-is-installed` | Main flow: TTY detection, prompt, conditional Pass 2 |
| `lib/render.sh` | Scan mode: version column = `?` |
| `tests/run.sh` | Adapt existing + 3 new tests |

## Dependencies

None. Self-contained phase.

## Verification

- [ ] TTY: scan table renders with `?`, prompt appears, 'y' triggers full table
- [ ] TTY: prompt 'n' or Enter → exits after scan
- [ ] Non-TTY: no prompt, scan only, exit 0
- [ ] Existing tests pass
- [ ] New tests pass
- [ ] Shellcheck clean
